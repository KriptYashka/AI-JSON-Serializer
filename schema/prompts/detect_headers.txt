Дан фрагмент XLSX-таблицы (первые строки).

{rows}

Определи, в какой строке находятся заголовки колонок (названия столбцов).
Верни ТОЛЬКО номер строки (число). Первая строка имеет номер 0.
Если заголовки не найдены — верни -1.
